# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
#command=$SCRIPT_DIR/$BLOCK_NAME
command=/usr/lib/i3blocks/$BLOCK_NAME
separator_block_width=10
markup=none

[MPD]
label=
command=mpc status | perl -ne 'if (/\[playing\]/) {CORE::say ("[",`mpc current|tr -d "\n"`,"]")}'
color=#a074ca
interval=5
signal=3

# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
[volume]
command=~/dotfiles-gh/i3blocks/scripts/volume
label=♪
instance=Master
#instance=PCM
interval=5
signal=2

[keyboard]
label=
command=~/dotfiles-gh/i3blocks/scripts/keyboard.sh
interval=once
signal=1

# Disk usage
#
# The directory defaults to $HOME if the instance is not specified.
# The script may be called with a optional argument to set the alert
# (defaults to 10 for 10%).
[disk]
command=~/dotfiles-gh/i3blocks/scripts/disk
label=
#instance=/mnt/data
interval=30

[disk2]
command=~/dotfiles-gh/i3blocks/scripts/disk2
label=
#instance=/mnt/data
interval=30
# Network interface monitoring
#
# If the instance is not specified, use the interface used for default route.
# The address can be forced to IPv4 or IPv6 with -4 or -6 switches.
[iface]
#instance=wlp7s0
color=#006F32
interval=10
separator=false

[wifi]
command=~/dotfiles-gh/i3blocks/scripts/wifi
label=
instance=wlp7s0
interval=10
separator=true


# Support multiple chips, though lm-sensors.
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
[temperature]
command=~/dotfiles-gh/i3blocks/scripts/temperature
label=
interval=10
separator=true

# Battery indicator
#
# The battery instance defaults to 0.
#[battery]
#command=~/dotfiles/i3blocks/scripts/battery
#label=⚡
##instance=1
#interval=30

[batterybar]
command=~/dotfiles-gh/i3blocks/scripts/batterybar  #run batterybar -h for options
label=⚡
interval=5
markup=pango
min_width= ⚡ ■■■■■

# Date Time
#
[time]
label=
command=date '+%Y-%m-%d %H:%M:%S'
interval=5
